steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - $_IMAGE
      - -f
      - Dockerfile.api
      - .
images:
  - $_IMAGE
substitutions:
  _IMAGE: banker-crm-api
options:
  logging: CLOUD_LOGGING_ONLY
